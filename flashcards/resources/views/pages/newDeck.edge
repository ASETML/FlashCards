@component('components/layout', { title: 'Login' })
<h1>Nouveau Deck</h1>
<form action="{{ route('createDecks')}}" method="POST">
  {{ csrfField() }}
  <div class="formLabel">
    <label>Titre</label>
    <input type="text" id="title" name="title" />
    <p id="errorMessage">{{titleError || ""}}</p>
  </div>
  <div class="formLabel">
    <label>Description</label>
    <input type="text" id="description" name="description" />
    <p id="errorMessage">{{descError || ""}}</p>
  </div>
  <div class="formLabel">
    <label>Nombre de cartes</label>
    <input type="number" id="difficulty" name="difficulty" />
  </div>
  <input type="submit" value="Ok" />
  <a href="{{ route('accueil') }}">Abandonner</a>
</form>

@endcomponent
